# Python WebSocket Server for Flutter

This project is a simple Python WebSocket server that receives commands from a Flutter Web client and responds in real-time.

## Requirements

- Python 3.7+
- websockets

## Install

```bash
pip install -r requirements.txt
